Place the OpenCV DNN face-detector model files here
(e.g. res10_300x300_ssd_iter_140000.caffemodel + deploy.prototxt,
or an equivalent small pre-trained face-detection model).
Bundled into the Docker image at build time so no download is needed
at runtime (keeps the tool fully offline-capable).
