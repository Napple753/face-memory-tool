// Pinia store: single source of truth for members, boxes, and column mapping.
// To implement: state (members, boxes, columnMapping), actions (assignName,
// addBox, removeBox, updateBoxPosition, markMissing, importFromJson, etc.)
